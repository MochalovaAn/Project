#include <stdio.h>
#include <string.h>

//int main()
//{
//    int cnt;
//    char smb;
//    char *code = new char [80];
//    char *encode = new char [80];
//    char *str = new char [80];
//
//    scanf("%s", code);
//
//    strcpy(encode, "");
//    smb = code[0];
//    cnt = 0;
//
//    for (int i = 0; i <= strlen(code); i++) {
//        if (code[i]==smb) {
//            cnt++;
//        }
//        else {
//            sprintf(str, "%d", cnt);
//            strcat(encode, str);
//            sprintf(str, "%c", smb);
//            strcat(encode, str);
//            smb = code[i];
//            cnt = 1;
//        }
//    }
//
//    printf("%s\n", encode);
//
//    return 0;
//}

size_t series_encode(char *buf_in, char *buf_out, size_t size)
{
    return 0;
}

size_t series_decode(char *buf_in, char *buf_out, size_t size)
{
    return 0;
}
